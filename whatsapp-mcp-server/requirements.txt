httpx>=0.28.1
# Capped below 2.0: it removes mcp.server.fastmcp, which main.py imports.
# Without the cap any fresh build picks up 2.0 and the server dies on import
# with ModuleNotFoundError — the running image only works because it was built
# before 2.0 was released.
mcp[cli]>=1.23.0,<2.0.0
requests>=2.32.3
starlette>=1.3.1
uvicorn>=0.23.2
urllib3>=2.6.3
h11>=0.16.0
gradio==6.14.0
gradio_client==1.10.3
qrcode[pil]>=8.0
